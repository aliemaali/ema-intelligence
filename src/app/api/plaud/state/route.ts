import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { CURRENT_PLAUD_NOTE } from '@/lib/plaud/current-note'
export async function GET(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'unauthorized'},{status:401});const[items,note]=await Promise.all([supabase.from('plaud_items').select('id,external_id,kind,title,detail,source,status,due_at').eq('user_id',user.id).order('created_at',{ascending:true}),supabase.from('plaud_notes').select('id,title,recorded_at,archived_at').eq('user_id',user.id).eq('external_id',CURRENT_PLAUD_NOTE.externalId).maybeSingle()]);if(items.error||note.error)return NextResponse.json({error:'PLAUD-Daten konnten nicht geladen werden.'},{status:500});return NextResponse.json({items:items.data??[],note:note.data??null})}
